import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'custom-checkbox-group',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
